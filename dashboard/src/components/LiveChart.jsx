import React from 'react'

// Props: history — array of { time, rps, blocked } snapshots (last N seconds)
export default function LiveChart({ history }) {
  // TODO: render Recharts LineChart with rps and blocked lines
  return <div>LiveChart — TODO</div>
}
