import React from 'react'

// Props: metrics { requestsPerSecond, totalRequests, blockedRequests,
//                  blockedRatio, avgLatencyMs, cacheHitRate }
export default function MetricsPanel({ metrics }) {
  // TODO: render 6 stat cards from metrics object
  return <div>MetricsPanel — TODO</div>
}
